#### 🗒️Instructions

This assignment is not graded, its primary purpose is to introduce you to the autograding process, which will be used throughout this course.

#### 📝 Test Assignment

Write a Python code that prints the following lines (exactly as shown, including punctuation):
```
Hello, User!
We are practicing.
```
Assign the value 1 to a variable called **test**, like `test = 1`. Using f-string print the following message by using the test variable as a number with 2 decimals.
```
This is test = 1.00.
```
Each line must be printed separately using its own `print()` call.

#### 🔒 Restrictions

* Use only basic **print()** statements—no external libraries.
* Each line must be printed separately using its own `print()` call.