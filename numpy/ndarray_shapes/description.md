#### 🎯 Task

Write a Python function `reshape_array()` that performs the following tasks:

- Creates a 1D NumPy array of integers from 1 to 12.
- **Reshapes** it into a 2D array with shape (3, 4).
- Prints the original array and the reshaped array.

#### 🔒 Restrictions

* **Use only numpy** 
* Follow the exact naming (`reshape_array()`)