#### 🎯 Task

Write a Python function named `matrix_operations()`. Inside the function:

- Create **Matrix A** with the following 3x2 structure:

    [[1, 2],

    [3, 4],

    [5, 6]]

- Create **Matrix B** with the following 2x3 structure:

    [[7, 8, 9],

    [10, 11, 12]]
    
- Create **Matrix C** as the **dot product** of A and B. 

- **Transpose** Matrix C to produce Matrix D.

The function shall **return** all four matrices.

**Call** the function and **print** the following matrices:
    Matrix A 
    Matrix B
    Matrix C
    Matrix D (Transpose of C)

#### 🔒 Restrictions

* **Use only numpy** 
* Follow the exact naming (`matrix_operations()`, `A`, `B`, `C`, `D`)