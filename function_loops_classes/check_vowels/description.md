#### 🎯 Task

1. Write a Python function called `contains_only_vowels(s)` that takes a string `s` as input and returns:
    * `"Only Vowels"` if the string contains **only vowels** (`a`, `e`, `i`, `o`, `u`, both lowercase and uppercase),
    * `"Not Only Vowels"` if the string contains **any other characters**.

2. Use the following **exact** list of test strings: <br>
`test_strings = ["eiou", "hello", "AaEeIiOoUu"]`<br>
Test your function with each of these strings and print the results.

#### 🔒 Restrictions

* **Do not use any external libraries** (e.g., no `re`, `string`, etc.).
* Your code should handle both **uppercase and lowercase** vowels.
* Follow the exact naming (`contains_only_vowels`, `test_strings`).