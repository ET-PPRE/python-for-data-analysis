#### 🎯 Task

1. Write a Python function called `check_even_odd(number)` that takes **one number as input** and returns:
    * `"Even"` if the number is **even**,
    * `"Odd"` if the number is **odd**.
    ups

2. Define a list `numbers_to_test ` with 5 numbers (integers) of your choice<br>

3. Create a loop which calls the function  `check_even_odd(number)` for each numberin the list and print the results.

#### 🔒 Restrictions

* **Do not use any external libraries**.
* Use basic Python operations to check whether a number is even or odd.
* Follow the exact naming (`check_even_odd`, `numbers_to_test`).